# Copy to backend/.env for local development. Never commit the real values.
GMAIL_EMAIL=your-gmail-address@example.com
GMAIL_PASSWORD=your-gmail-app-password

# For Render, this is supplied by render.yaml and points to its persistent disk.
DATABASE_PATH=/var/data/spam_detection.db
